apiVersion: v1
kind: Service
metadata:
  name: worker-metrics
  namespace: sb-ledger
  labels:
    app: worker-metrics
spec:
  selector:
    app: worker
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
